services:
  - type: web
    name: php-chat-app
    env: python
    plan: free
    buildCommand: chmod +x start.sh && ./start.sh setup
    startCommand: ./start.sh
    envVars:
      - key: PORT
        value: 10000